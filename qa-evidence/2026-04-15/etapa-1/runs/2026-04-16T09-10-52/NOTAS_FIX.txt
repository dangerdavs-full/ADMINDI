Cambios aplicados para smoke Etapa 1 integral (2026-04-16)
============================================================

1. API REST de preferencias de notificación (antes inexistente en controladores):
   - GET  /api/notifications/preferences
   - PUT  /api/notifications/preferences  (cuerpo: { "preferences": [ { "eventType", "channel", "enabled" } ] })
   - Archivos: NotificationPreferenceDTO.java, NotificationController.java

2. scripts/etapa1-smoke.ps1 — ampliación integral:
   - Soporte HTTP PUT en Invoke-Api.
   - Fase 7: tenant multi-owner (qa.etapa0.tenant.multi@test.local) → GET /auth/contexts (>=2),
     POST /auth/select-context, GET /tenant/expedientes, POST /auth/switch-context, GET /tenant/expedientes.
   - Fase 8: GET /tasks/open-count, GET /tasks?status=OPEN, GET /notifications, unread-count,
     GET/PUT /notifications/preferences.
   - Veredicto verde: ETAPA1_SMOKE_RESULT=PASSED_ETAPA1_INTEGRAL_QA
   - Código de salida: 0 si todo pasa, 1 si hay issues (antes siempre 0).

3. Frontend — centro de preferencias (sin placeholder):
   - notificationPreferenceService.ts, NotificationPreferencesPanel.tsx
   - Pestaña "Alertas y canales" en OwnerDashboardLayout (solo OWNER / contexto con permisos de dueño en layout actual).

Runtime de esta corrida
------------------------
- Backend de verificación: Spring Boot perfil `qa`, puerto 8081 (instancia temporal; no el 8080 del IDE),
  ADMINDI_QA_SEED_ENABLED=true, misma base PostgreSQL local.
- SMOKE_BASE_URL=http://localhost:8081/api
