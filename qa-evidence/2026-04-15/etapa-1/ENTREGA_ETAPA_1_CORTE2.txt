Etapa 0 sigue BLOQUEADA_PROD por ENCRYPTION_KEY / validación prod-like de Etapa 0, pero no bloquea avance funcional de Etapas 1–5.

================================================================================
Etapa exacta: 1 — Alineación TenantController + plantilla property admin + smoke PROP_ADMIN (corte 2)
================================================================================

STATUS_LOCAL_QA=YELLOW
STATUS_PROD=BLOCKED_BY_STAGE0

Bloqueos globales heredados
---------------------------
- `qa-evidence/2026-04-15/BLOQUEOS_GLOBALES_PROD.txt`

Implementado en código
----------------------
- **TenantController:** `DELETE /api/tenants/{id}` y `POST /api/tenants/{id}/archive` exigen `PROPERTY_ARCHIVE_TENANT` o `TENANT_DELETE` (además OWNER/SUPER_ADMIN). `PUT` y altas/listados siguen con `TENANT_UPDATE` / `TENANT_CREATE` / `TENANT_VIEW`.
- **JwtAuthenticationFilter:** token **FULL** fusiona la claim `permissions` del JWT en las `GrantedAuthority` del `Authentication`, para que `@PreAuthorize(hasAuthority(...))` refleje el grant por contexto (antes solo se leían permisos en BD `user_permissions`, inútil para staff con plantilla).
- **Flyway V28:** actualiza `tpl-property-admin-operational` — nombre “expediente y archivo”, JSON con `TENANT_VIEW`, `TENANT_UPDATE`, `TENANT_CREATE`, `PROPERTY_ARCHIVE_TENANT` y permisos de corte previo.
- **QaEtapa0SeedService:** Property admin + Owner Alpha → un solo grant `tpl-property-admin-operational` (reemplazo idempotente); Bravo → `tpl-full-access`.
- **scripts/etapa0-smoke.ps1** (fase `SMOKE_FULL_ETAPA0`): tras MFA prop admin, `contextId` de Alpha desde `GET /auth/contexts` (match insensible a mayúsculas del nombre, default `QA Owner Alpha`; overrides `SMOKE_PROPADMIN_ALPHA_CONTEXT_NAME` / `SMOKE_OWNER_ALPHA_ID`), luego `select-context`, `GET /tenants`, `DELETE` dummy → **400** = permiso OK; **403** = `PROPADMIN_ARCHIVE_PERM`; sin match = `PROPADMIN_ALPHA_CONTEXT_NOT_FOUND`.

Validado en runtime
-------------------
- `mvn compile -DskipTests` en backend (agente).
- **Tu entorno:** `flyway migrate` (V28) + reinicio + semilla QA; luego `SMOKE_FULL_ETAPA0=1` + TOTP con `etapa0-smoke.ps1`.

Pendiente
---------
- Cobertura automática MockMvc/MVC test para la fusión JWT + un endpoint `hasAuthority`.
- Revisar otros controladores que asuman permisos solo en BD.

Riesgos
-------
- Cualquier cliente que confiara solo en `TENANT_UPDATE` para `POST .../archive` debe migrar a `PROPERTY_ARCHIVE_TENANT` o `TENANT_DELETE` (DELETE y POST archivo alineados).
- `replaceGrantsWithSingleTemplate` en semilla borra grants duplicados Alpha+prop admin en cada arranque con semilla (deseable para QA).

Evidencia exacta
----------------
- `backend/src/main/java/com/admindi/backend/controller/TenantController.java`
- `backend/src/main/java/com/admindi/backend/security/JwtAuthenticationFilter.java`
- `backend/src/main/resources/db/migration/V28__property_admin_template_tenant_perms.sql`
- `backend/src/main/java/com/admindi/backend/qa/QaEtapa0SeedService.java`
- `scripts/etapa0-smoke.ps1`, `scripts/QA_SEED_USERS_README.txt`
- Este archivo
