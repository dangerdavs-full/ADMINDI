ADMINDI — Etapa 0 (Saneamiento y base real)
Fuente de verdad: ADMINDI_PLAN_FINAL_DEFINITIVO.md (gana sobre PLAN_FINAL_5_ETAPAS.md si hay contradicción)
Fecha evidencia: 2026-04-15

=== Tabla estado entorno (esta corrida) ===
Componente              | Resultado
-------------------------|--------------------------------------------------
PostgreSQL localhost     | Conectado (propgest_db); Flyway schema version 26
Flyway validate/migrate  | OK; "Schema up to date"; 26 migraciones Success
SQL migrations UTF-8 BOM | 25 archivos .sql: sin BOM UTF-8, sin UTF-16LE/BE (script PowerShell bytes)
Versiones Flyway         | V1..V26 sin duplicados; V25=JDBC Java, V26=SQL
Backend mvn compile      | OK
Frontend npm run build   | OK (tsc + vite)
Backend arranque (log)   | Evidencia terminal: Tomcat 8080 Started; Flyway validate; AdminSeeder; shutdown limpio
Redis                    | No verificado PING en esta corrida (app config localhost:6379)
R2 / object storage      | No en application.yml; storage local uploads (STORAGE_PATH)
n8n                      | integrations.n8n.enabled default false — no rompe arranque
ENCRYPTION_KEY           | Vacío en default: log WARN cifrado deshabilitado (dev); prod requiere clave per EncryptionService

=== Migraciones aplicadas (flyway:info) ===
Version 1-26 todas State=Success. Schema version 26.

=== Endpoints auth contexto (código) ===
GET  /api/auth/contexts
POST /api/auth/select-context
POST /api/auth/switch-context   (alias añadido 2026-04-15 = mismo handler que select-context, alineado a plan maestro)
POST /api/auth/refresh

=== Ajuste código esta sesión ===
- AuthController: POST /api/auth/switch-context
- JwtAuthenticationFilter: permitir token BASE en /auth/switch-context
- RateLimitInterceptor: mismo bucket que select-context para switch-context

=== Etapa cerrada o bloqueada ===
VEREDICTO: BLOQUEADA (no cierre formal Etapa 0 completo según criterio de gobierno propio)

Motivo: el checklist exige evidencia operativa de auth (login, MFA, refresh, revoke, revocación inmediata al archivar tenant, logs sin errores bloqueantes en regresión completa). Esta entrega documenta migraciones + builds + Flyway + arranque puntual en log; NO incluye suite de pruebas manuales/automáticas firmadas de todos los ítems 3 y 4.

=== Bloqueos reales ===
1. Evidencia explícita pendiente: flujo login+MFA+contexts+select+switch+refresh+revoke en video/log HTTP capturado.
2. Redis: confirmar conectividad y comportamiento refresh reuse en entorno CI/staging.
3. R2: no hay integración en yml actual; plan stack menciona R2 — definir mock/prod y documentar por entorno.
4. PostgreSQL 18 vs Flyway: WARNING "upgrade recommended" (no bloquea migrate en prueba).

Siguiente acción recomendada: repetir Etapa 0 con script de smoke (curl/REST) y adjuntar capturas; luego Etapa 1 con misma plantilla de entrega.
